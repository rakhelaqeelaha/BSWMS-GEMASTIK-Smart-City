
  # Aplikasi untuk itu

  This is a code bundle for Aplikasi untuk itu. The original project is available at https://www.figma.com/design/LT8DL8eA6RHKKc4ysAKt30/Aplikasi-untuk-itu.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  